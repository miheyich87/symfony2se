
                {%- for action in record_actions %}

                        <a class="btn btn-default" rel="tooltip" title="{{ action }}" href="{{ "{{ path('" ~ route_name_prefix ~ "_" ~ action ~ "', { 'id': entity.id }) }}" }}">{{ action }}</a>

                {%- endfor %}